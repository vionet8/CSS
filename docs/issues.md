# 課題管理台帳（Issues Ledger）

**最終更新**: 2026-07-07（セールス・マーケ第一戦略の対応を追記）
**目的**: コードベース監査で検出した問題点・課題・未達成項目を一元管理する。
新しい課題を見つけたらここに追記し、解決したら「✅ 解決済み」に移す。

---

## ✅ 解決済み（2026-07-07 監査で検出・修正）

### バグ

| # | 深刻度 | 内容 | 修正 |
|---|--------|------|------|
| B1 | 高 | `PATCH /projects/{id}/slides/{slide_id}` がスライド dict をその場で更新していたため、SQLAlchemy が変更を検知できず**編集がDBに保存されないことがある** | 新しい dict/list を作って代入する方式に変更。回帰テスト `test_update_slide_persists` 追加 |
| B2 | 高 | 画像API（`/images/file/{filename}`・resize・crop・split）がファイル名を検証せず、`../` を含む名前で**アップロードディレクトリ外のファイルにアクセス可能**（パストラバーサル） | `safe_filename()` で `/` `\` `..` 先頭ドットを拒否。テスト `test_path_traversal_rejected` 追加 |
| B3 | 中 | スライド生成・AI改善のルートに例外処理がなく、AI応答異常時に生の500エラーが返る | try/except を追加し、AI起因は502 + 日本語メッセージに統一 |
| B4 | 中 | `improve_slide` だけコードブロック除去・json_repair フォールバックがなく、AI応答形式が崩れると必ず失敗 | JSONパースを `_parse_ai_json()` に共通化。改善時に `id`/`order` が書き換わらないよう保護も追加 |
| B5 | 中 | フロントの構造分析・スライド生成が**失敗してもタブが切り替わる**（ストアがエラーを握りつぶすため） | ストアのアクションが成功/失敗を返すようにし、成功時のみタブ遷移 |
| B6 | 中 | URL取得・PDF読込・スライド保存系ハンドラ（SlideCard の5関数）に catch がなく、失敗が**ユーザーに一切通知されない** | 全ハンドラにエラー通知を追加。axios エラー整形を `errorDetail()` に共通化 |
| B7 | 低 | `tsc --noEmit` が `import.meta.env` で3件エラー（`vite-env.d.ts` 欠落）。ビルドスクリプトも型チェックを実行していなかった | `vite-env.d.ts` 追加、`npm run build` に `tsc --noEmit` を組み込み |
| B8 | 低 | PDFアップロードが `file.filename.endswith(".pdf")` のため、ファイル名が `None` でクラッシュ・`.PDF`（大文字）が拒否される | None安全 + 小文字化して判定 |
| B9 | 低 | 画像アップロードが拡張子・サイズ・画像としての妥当性を検証せず、任意ファイルを保存できた | 拡張子ホワイトリスト・20MB上限・PIL verify を追加 |
| B10 | 低 | `except (ValueError, Exception)` という冗長な例外捕捉、非推奨 `@app.on_event("startup")`、Pydantic v1形式 `class Config` | それぞれ整理（lifespan 移行、`SettingsConfigDict` 化） |

### 設計・保守性の課題

| # | 内容 | 対応 |
|---|------|------|
| D1 | テストが1件も存在しない | pytest + 一時SQLite + AIモックで**テスト30件**を整備（`backend/tests/`） |
| D2 | CIがない（壊れたコードがそのままpushできる） | GitHub Actions を追加（backend: pytest / frontend: 型チェック+ビルド） |
| D3 | Claudeモデル名が3箇所にハードコード | `CLAUDE_MODEL` 環境変数に集約（デフォルト: claude-sonnet-4-6） |
| D4 | DB接続設定が `config.py` と `database.py` で二重管理 | `settings.DATABASE_URL` に一本化 |
| D5 | API キー未設定時に不可解なエラーになる | 呼び出し前にチェックし「ANTHROPIC_API_KEY が未設定です」と明示 |
| D6 | `fetch-url` が任意スキーム（file:// 等）を受け付ける | http/https のみ許可 |
| D7 | プロジェクト削除が確認なしで即実行される | 確認ダイアログを追加 |

### 未達成項目の実装

| 項目 | 状態 |
|------|------|
| プロジェクトのエクスポート（JSONダウンロード） | ✅ 実装（`GET /projects/{id}/export` + 一覧のダウンロードボタン） |
| プロジェクトのインポート（JSONアップロード） | ✅ 実装（`POST /projects/import` + 一覧のインポートボタン、往復テスト付き） |

### セールス・マーケ第一戦略のギャップ（2026-07-07 対応）

**戦略**: CSSは「これまで・これからのプロダクトのセールス/マーケを簡単にするツール」。
その観点で監査した結果、以下のギャップを特定し実装した。

| # | ギャップ | 対応 |
|---|----------|------|
| M1 | 構造分析が汎用フレーム（現状/問題/原因…）のみで、販促文脈の型がない | セールスフレームワーク分析を追加: **PASONA / AIDMA / FAB**（`POST /projects/{id}/marketing/analyze`）。結果はロジック構造タブにフレームワークバッジ付きで表示 |
| M2 | プロダクト名・ターゲット顧客・訴求ゴール・トーンを保持する場所がない | **プロダクトプロファイル**を追加（`PUT /projects/{id}/marketing/profile`、`assets.marketing_profile` に保存）。分析・スライド生成・素材生成すべてに自動反映 |
| M3 | アウトプットがスライドのみ。「相互変換」理念に反して販促チャネル向け素材に変換できない | **販促素材生成**を追加: X投稿×3案 / LPコピー一式 / セールスメール×2案 / プレスリリース / 広告コピー×5案（`POST /projects/{id}/marketing/assets`、`assets.marketing_assets` に永続化、UIにコピー・再生成・削除） |
| M4 | スライド生成がセールスピッチを意識しない（CTAで締まらない） | プロファイル設定時は生成プロンプトにセールス文脈を注入し、最終スライドをCTAで締めるよう指示 |
| M5 | 未使用だった `assets` JSONカラム | マーケデータの保存先として活用（スキーマ変更・マイグレーション不要） |

テスト13件を追加（`tests/test_marketing.py`）: フレームワーク/素材タイプ検証、プロファイル永続化、素材の追加が既存素材を壊さない回帰、構造→素材テキスト変換、スライド生成へのプロファイル伝播。

### LP第一ワークフローの強化（2026-07-07 追加）

「LPを最初に作る」進め方に合わせて、以下を追加実装した。

| # | ギャップ | 対応 |
|---|----------|------|
| M6 | LPや素材にターゲット設計が既に書かれている場合、それを手で転記して評価する必要があった | **プロファイル審査**を追加（`POST /projects/{id}/marketing/profile/audit`）。素材からプロダクト名・ターゲット・ゴール・トーンを自動抽出し、項目ごとに OK/曖昧/欠落 で審査。読み取れた値はワンクリックでフォームに反映（保存はユーザー確認後） |
| M7 | ターゲット設計が素材から読み取れない場合に、何を決めるべきかの導きがなかった | 審査結果に**確認質問**を含める（例:「読者に最終的に取ってほしい行動は？」）。欠落項目は黄色の確認ボックスで表示 |
| M8 | ChatGPT等の外部AIで作った素材を活かす経路がなく、貼り付けても丸ごと1テキスト扱いだった | **外部素材の取り込み・分解**を追加（`POST /projects/{id}/marketing/materials`）。素材を断片（主張/ベネフィット/根拠・実績/事例/キャッチコピー/CTA）に分解して蓄積し、以降の構造分析・素材生成プロンプトに自動で散りばめる。断片は個別削除可、保存上限200件・プロンプト注入上限40件 |

テスト8件を追加（`tests/test_marketing_materials.py`）。合計50件。

| # | ギャップ | 対応 |
|---|----------|------|
| M9 | キャッチコピーだけを大量に出して比較・選定する使い方ができなかった | 素材タイプ「**キャッチコピー**」を追加。**切り口の異なる10案**（ベネフィット直球/数字・実績/問いかけ/損失回避/ターゲット呼びかけ/意外性・逆説/比喩/社会的証明/緊急性・限定/情緒・共感）を各テーマ名付きで生成。素材に実績数値がない場合は捏造せず別表現に置き換えるガード付き |
| M10 | LPコピー（文章）までしか作れず、公開できるLP本体は手作業だった | **LP HTML書き出し**を追加（`GET /projects/{id}/marketing/lp.html`）。生成済みLPコピーから、ヒーロー→ベネフィット→CTAの一枚もの・レスポンシブ・OGP付きのセルフコンテインドHTMLをテンプレートで決定的に生成。CTAリンクとアクセント色を指定でき、UIからプレビュー・ダウンロード可能。コピーはHTMLエスケープ、CTA URLは http(s)/相対のみ許可（javascript:等を拒否）、アクセントは#RRGGBB検証 |
| M12 | スライド→動画が完全未着手（Phase 2/3） | **動画パイプライン**を実装: スライドをブラウザで1280x720 PNG化→アップロード（`POST /video/frames`）、発表者ノートを**VOICEVOX**（ずんだもん=3/めたん=2）で音声合成、**FFmpeg**でmp4合成＋実測長のSRT字幕（`POST /video/render`）。ノート無しスライドは2秒無音。SRT単体出力も可（`GET /video/subtitles.srt`、ツール不要）。FFmpeg/VOICEVOXは起動時検出し、無ければ導入方法を案内（`GET /video/tools`）。テスト10件追加（合計75件） |
| M13 | Phase 3.5（動画取り込み）が未着手で、過去の動画資産を素材化できなかった | **メディア取り込み**を実装: 動画/音声アップロード（500MBまでストリーミング保存）、FFmpegで前後カット・音声抽出（16kHz WAV）、**faster-whisper**（任意依存 `requirements-media.txt`）で文字起こし（テキスト+セグメント+SRT）。入力タブの「動画/音声」ボタンから文字起こし→コンテンツに自動追記→そのまま構造分析へ。ツール未導入時は導入方法を案内（`GET /media/tools`）。テスト8件追加（合計83件） |
| M14 | Phase 4（人気動画分析）が未着手 | **YouTube構成分析**を実装（`POST /projects/{id}/youtube/analyze`）: yt-dlpでメタデータ・字幕を取得（動画本体は非ダウンロード、日本語字幕優先/自動字幕フォールバック）、AIが時間配分（積み上げバー表示）・フック・CTA・転用できる学びを分析。「動画分析」タブで保存・削除、文字起こしを入力タブへ送って自分のコンテンツの素材化も可能。テスト6件追加（合計89件） |
| M15 | Phase 5（認知変化・FPRL分析）が未着手 | **FPRL認知変化分析**を実装（`POST /projects/{id}/fprl/analyze`、ユーザー提供のFPRL理論PDFに基づく）: コンテンツを「読者のF（基準）を書き換える設計」として審査。認知変化マップ（Before F/行動 → After F/行動）、E₁気づき→E₂トリアージ→R推論→E₃受容→L定着の5ゲート通過審査（該当箇所の引用付き）、停滞ポイント＝論理の穴と具体的修正案を出力。「認知変化」タブで表示、マーケプロファイルを審査に反映。テスト5件追加（合計94件） |
| M17 | キャラクターがVOICEVOX系（ずんだもん/めたん）に固定されており、ゆっくり系（れいむ/まりさ）等の別音声エンジンを追加できなかった | **音声エンジンの抽象化**を実装: `CHARACTER_VOICES` でキャラごとに `voicevox`（HTTP API）/ `aquestalk`（AquesTalkPlayer.exeをサブプロセス実行）を切替。れいむ=aquestalk「れいむ」プリセット、まりさ=aquestalk「まりさ」プリセットを追加。`/video/tools` は音声エンジンごとの利用可否とライセンス注記込みの導入案内を返し、`/video/render` は選択キャラに必要なエンジンのみを要求（voicevoxキャラの動画生成にaquestalk未導入でも支障なし）。**AquesTalkPlayerは個人非営利無料・商用利用は別途ライセンス購入が必要**（https://store.a-quest.com/categories/618932）である点を導入ヒントに明記。テスト7件追加（合計101件）。れいむ/まりさの立ち絵画像・character_service.pyへの登録は、ユーザーが立ち絵素材（PSDまたはPNGセット）を用意次第、追加対応 |
| M16 | スライド・動画が「情報は正しいが目を引かない」— 視線誘導・感情要素の演出装置がなかった | **注目演出システム**を実装: ①集中線（漫画的バースト・視線を中央へ）②「重要」赤スタンプ ③スポットライト（周囲減光）④**キャラ吹き出し**（ずんだもん/めたんの一言ツッコミ、`character_line`）。SlideRendererの960x540レイヤー内で描画するため**PNG書き出し・動画・全画面プレゼンすべてに焼き込まれる**。スライド生成AIが自動配置（burst=冒頭フック、stamp=最重要1枚、spotlight=結論、乱用防止3割ルール）し、SlideCardの「演出」ボタンで手動調整・セリフ編集も可能。キャラ表示は`CharacterOverlay`に共通化 |
| M11 | LPテンプレートが1種のみで、商材・目的に合わせたレイアウトを選べなかった | **LPテンプレート3種**: standard（カード型）/ salesletter（縦長・構造分析ノードを本文セクション化、ベネフィットはチェックリスト）/ minimal（キャッチコピー特化1画面型）。**キャッチコピー差し替え**: 10案から選んだ案をヒーロー見出しに適用（`catchcopy_index`）。テスト7件追加（合計65件） |

---

## 🔲 未解決の課題（優先度順）

### 機能（docs/next_actions.md のフェーズ計画より）

| 優先度 | 項目 | メモ |
|--------|------|------|
| 高 | スライドエクスポート（PNG / PDF） | 販促資料として配布するために最重要。html2canvas + jspdf でPDF → その後PPTX検討 |
| 高 | プロダクトプロファイルのプロジェクト間共有 | 現在はプロジェクト単位。同一プロダクトの複数キャンペーンで使い回せるプロダクトマスタ化 |

| 高 | スライドDnD並べ替え | `@dnd-kit` は導入済みだが未使用。`order` 更新APIはあるのでフロント実装のみ |
| 中 | 素材の履歴・A/Bバリエーション管理 | 現在は素材タイプごとに最新1件のみ保存（再生成で上書き） |
| 中 | 競合・市場コンテキストの取り込み | fetch-url で競合LPを取得→差別化ポイントを構造分析に反映する流れの定型化 |
| 中 | Phase 2: FFmpeg 動画ユーティリティ | 未着手 |
| 中 | Phase 3: Whisper 文字起こし・字幕 | 未着手（推奨モデル: small） |
| 低 | Phase 4: YouTube 人気動画分析（yt-dlp） | 未着手 |
| 低 | Phase 5: 認知変化・FPRL 分析 | 未着手 |
| 低 | スライドのテーマ切替 / 画像割り当てUI / Markdownプレビュー / OCR | 細かい改善Todo |

### 技術的負債

| 項目 | メモ |
|------|------|
| PyPDF2 が非推奨 | 後継の `pypdf` へ移行する（DeprecationWarning が出ている） |
| `alert()` によるエラー通知 | トースト通知コンポーネントに置き換えるとUX向上 |
| フロントにユニットテストがない | Vitest 導入を検討（現状は型チェック+ビルドのみCIで担保） |
| ESLint / ruff などのリンタ未導入 | CIに追加するとさらに堅牢に |
| 画像に孤児ファイルが溜まる | プロジェクト削除時に関連画像を消す仕組みがない |
| requirements の `opencv-python-headless` / `psd-tools` | 現状コードで未使用。依存を減らすか用途を明確化 |

---

## 運用ルール（課題管理の仕組み）

1. **課題を見つけたら**: この台帳の「未解決」に1行追加する（GitHub Issue を立てた場合はリンクを貼る）
2. **着手するとき**: ブランチを切り、対応するテストを先に書く
3. **解決したら**: CI が緑であることを確認し、「解決済み」セクションへ移動
4. **リリース前チェック**: `backend: python -m pytest` / `frontend: npm run build` が両方通ること（CIが自動で検証）
